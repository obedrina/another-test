version: v1.0
name: Initial Pipeline
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu2004
global_job_config:
  prologue:
    commands:
      - echo cole
blocks:
  - name: 'Block #1'
    task:
      jobs:
        - name: 'Job #1'
          commands:
            - checkout
  - name: 'Block #2'
    task:
      jobs:
        - name: 'Job #1'
          commands:
            - echo test > test.txt
            - artifact push workflow test.txt
  - name: 'Block #3'
    task:
      jobs:
        - name: 'Job #1'
          commands:
            - cat /tmp/system-metrics
